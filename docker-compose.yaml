version: '3.1'

services:

  postgres:
    container_name: postgres
    image: postgres:15.1
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_PORT=5432
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

  redis:
    image: redis:latest
    hostname: redis
    ports:
      - "6379:6379" # убрать когда будет все докирезировано

  worker:
    build:
      context: .
    image: cmc_celery_dev:latest
    hostname: worker
    entrypoint: celery
    command: -A celery_app.app worker --loglevel=info
    volumes:
      - .:/cmc
    links:
      - redis
    depends_on:
      - redis
      - postgres
    environment:
      - DB_HOST=localhost
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_NAME=${POSTGRES_DB}


  flower:
    build:
      context: .
    image: cmc_flower_dev:latest
    hostname: flower
    entrypoint: celery
    command: -A celery_app.app flower
    volumes:
      - .:/cmc
    links:
      - redis
    depends_on:
      - redis
    ports:
      - "5555:5555"


volumes:
  postgres_data:
    driver: local
